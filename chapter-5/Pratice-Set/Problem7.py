s = {8, 7, 12, "Harry", [1,2]}

# We dont include a list inside the set 

# Sets in Python required all their element to be immutable and hashable . List are mutable and hashable so they cannot added in to the sets 